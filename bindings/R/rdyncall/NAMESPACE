useDynLib(rdyncall)
export(
  # dyncall.R
  .dyncall, .callmodes, .dyncall.cdecl, .dyncall.stdcall,
  # dynload.R
  .dynload, .dynunload, .dynsym,
  # dynfind.R
  dynfind, pathsFromEnv,
  # dynbind.R 
  dynbind,
  # dynstruct.R 
  new.struct, as.struct, parseStructInfos, parseUnionInfos, 
  align, getTypeInfo,
  # pack.R
  .pack1, .unpack1, 
  # callback.R
  new.callback,
  # utils.R 
  .addrval, is.externalptr, is.nullptr, offsetPtr,
  # dynport.R
  dynport, loadDynportNamespace
)

S3method("$<-", struct)
S3method("$", struct)
S3method(print,struct)

